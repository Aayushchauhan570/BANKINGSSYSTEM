{
  "name": "banking-system",
  "version": "1.0.0",
  "description": "Full-stack banking system with React frontend and Express backend",
  "type": "module",
  "scripts": {
    "dev": "npm install && cd Backend && npm install && cd ../Frontend && npm install",
    "start:backend": "cd Backend && npm start",
    "start:frontend": "cd Frontend && npm run dev",
    "start": "cd Backend && npm start",
    "build": "cd Frontend && npm run build"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "react-router-dom": "^7.9.5"
  }
}
